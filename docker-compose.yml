version: '2'
services:
    example.com:
        image: skilgarriff/alpine-nginx
        volumes:
            - ./zzrot.com/public:/var/www/html:ro
        networks:
            - frontend
networks:
    frontend:
