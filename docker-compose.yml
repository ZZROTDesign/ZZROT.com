version: '2'
services:
    zzrot.com:
        image: skilgarriff/alpine-caddy
        volumes:
            -   ./Caddyfile:/etc/Caddyfile
            -   ./zzrot.com/public:/var/www/html
        networks:
            -   frontend
    blog.zzrot.com:
        image: ghost
        networks:
          -   frontend

networks:
    frontend:
